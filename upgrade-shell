### Upgrade Shell

python -c 'import pty; pty.spawn("bash")' || python3 -c 'import pty; pty.spawn("bash")'

Ctrl - Z

stty raw -echo ; fg ; reset

stty columns 200 rows 200

export TERM=xterm-256color

